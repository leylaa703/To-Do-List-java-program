package todoapp.service;

/**
 * Task filtering options for display
 * Controls which tasks are shown based on status and due date
 */
public enum TaskFilter {
    ALL,
    COMPLETE,
    INCOMPLETE,
    OVERDUE
}
